Screen11 As screen:

    Button2 As button:
        OnSelect: |
            =newFlow.Run(money.Text, project.Text)
        Text: ="mandar correo"
        X: =594
        Y: =615
        ZIndex: =1

    Button1 As button:
        Height: =92
        OnSelect: |+
            =ClearCollect(colData, {
                money: money.Text, 
                project: project.Text
                }
            );
            
            Set(data, JSON(colData));
            dataFlow.Run(data)
            
            
            
        Text: |-
            ="Recoger datos (JSON)
            "
        X: =594
        Y: =307
        ZIndex: =2

